<%= render partial: 'shared/navbar' %>

<%= render partial: 'shared/list', locals: { services: @services } %>

<div class="flex flex-1 justify-center">
  <%= paginate @services %>
</div>

<%= render partial: 'shared/footer' %>
